---
description: Begin a work session — load context, surface where things stand, set a concrete plan. The convergence mandate is in force from here.
argument-hint: "[what you're working on]"
---

Starting work on: **$ARGUMENTS**

1. Load the relevant context: for 命理/destiny work read `.claude/canon/canon.md`; otherwise the relevant `docs/` + `skills/`. Check `.session-notes` (or the workspace note) and any in-progress tasks; summarise where things stand in 2–3 lines.
2. State a concrete plan (the steps). Lead with the plan; ask only if genuinely blocked.
3. Confirm the operating rules for this session:
   - **Convergence mandate** — every substantive step is vetted/red-teamed to convergence before it's called done (`rules/redteam-mandatory.md`); the `vet-gate` Stop hook enforces it.
   - **Calculators only** for any chart value (`.claude/calc/`), never hand-math (`rules/calc-authority.md`).
   - **Accuracy** — every generated claim traces to a source in `docs/` or is flagged `[UNVERIFIED]`.
