---
description: Phase 05 — capture durable, verified knowledge from this work into the permanent KB for future sessions.
argument-hint: "[what to codify]"
---

## Do
- Promote durable, **verified** findings from the workspace into the permanent KB: new/updated `docs/` (the right `00-anchor`…`08-personal` section), the profile/standards skills, or a new rule/instinct.
- **Source everything** — never codify an unverified claim. If a finding touches the canon, amend it via the ceremony (`rules/canon-protection.md`), never silently.
- Update the relevant `docs/` index; note what changed and why.
- Capture reusable process lessons / new instincts (`rules/learned-instincts.md` is auto-generated by the session-end hook; write deliberate rules explicitly).

## Gate
`/vet` the codified changes — sourced, consistent, no contradiction — before reporting complete (`rules/redteam-mandatory.md`).
