# GENERATED by .claude/bin/emit-cli-artifacts.mjs — DO NOT EDIT. Edit the .claude/ source and re-run.
#
# AGENTS.md carries the project instructions PLUS all inlined global rules, which
# exceeds Codex's 32,768-byte default project-doc cap (currently 59449 B).
# Without this raise, Codex silently TRUNCATES the governance rules — including the
# canon-protection and convergence rules — so this value is load-bearing, not cosmetic.
#
# DERIVED from the measured AGENTS.md size x1.25 headroom, rounded up to a
# 65536 B boundary, floor 65536 B. Do not hand-pin it: it must track the file.
project_doc_max_bytes = 131072
