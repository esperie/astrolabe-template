# docs/ — Personal Knowledge Base

This is the instance owner's private knowledge base. Organize it however suits the owner; a common
starting layout:

| Folder | Holds |
|---|---|
| `00-anchor/` | Identity, vision, the throughline |
| `01-…/` | One folder per life/work domain (career, projects, relationships, health, finances…) |

**Rules that apply here:**
- Every factual claim used in generated content must trace to a file here (`rules/branding`-style
  accuracy; Absolute Directive #1).
- No stubs/placeholders in live content (`rules/no-stubs.md`).
- Keep it private (`rules/security.md`).

Onboarding seeds `00-anchor/`. Grow the rest as you go.
